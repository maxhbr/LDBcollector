= *Allowed* Licenses

This page lists the licenses that are *allowed* for use in Fedora. If a license is marked as *allowed*, it is allowed for any use. If a license is marked as *allowed-fonts*, *allowed-content*, *allowed-documentation*, or *allowed-firmware*, then it is allowed only for certain categories of material as described in xref:license-approval.adoc[License Approval]. This information is generated from data maintained in the https://gitlab.com/fedora/legal/fedora-license-data[Fedora License Data] repository.

+++
<table id="allowed" class="display" style="width:100%">
        <thead>
            <tr>
                <th>SPDX Expression</th>
                <th>Name</th>
                <th>Fedora Abbreviation</th>
                <th>URLs</th>
            </tr>
        </thead>
        <tbody>
{%- for license in ALLOWED_LICENSES -%}
            <tr>
               <td><a href="{{ spdx_url(license) }}">{{ license['license']['expression'] }}</a></td>
               <td>
                {%- if 'fedora' in license -%}
                        {{ license['fedora']['name']|join(',') }}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
               <td>
                {%- if 'fedora' in license -%}
                        {{ license['fedora']['abbreviation']|join(',') }}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
                <td>
                {{ additional_urls(license) }}
                </td>
            </tr>
{%- endfor -%}
 </tbody>
        <tfoot>
            <tr>
                <th>SPDX Expression</th>
                <th>Name</th>
                <th>Fedora Abbreviation</th>
                <th>URLs</th>
            </tr>
        </tfoot>
    </table>
+++


= *Allowed* Content Licenses


= *Allowed* Documentation Licenses


= *Allowed* Font Licenses


= *Allowed* Firmware Licenses



= All licenses


+++                                                                                                                                                                                          
<table id="all" class="display" style="width:100%">
        <thead>
            <tr>
                <th>Allowed</th>
                <th>Allowed-content</th>
                <th>Allowed-documentation</th>
                <th>Allowed-fonts</th>
                <th>Allowed-firmware</th>
                <th>Status</th>
                <th>SPDX Expression</th>
                <th>Name</th>
                <th>Fedora Abbreviation</th>
                <th>URLs</th>
            </tr>
        </thead>
        <tbody>
{%- for license in ALL_ALLOWED_LICENSES -%}
            <tr>
               <td>
                {%- if 'allowed' in license['license']['status'] -%}
                        &#10004;
                {%- else -%}
                        &nbsp;
                {%- endif -%}
               </td>
               <td>
                {%- if 'allowed-content' in license['license']['status'] -%}
                        &#10004;
                {%- else -%}
                        &nbsp;
                {%- endif -%}
               </td>
               <td>
                {%- if 'allowed-documentation' in license['license']['status'] -%}
                        &#10004;
                {%- else -%}
                        &nbsp;
                {%- endif -%}
               </td>
               <td>
                {%- if 'allowed-fonts' in license['license']['status'] -%}
                        &#10004;
                {%- else -%}
                        &nbsp;
                {%- endif -%}
               </td>
               <td>
                {%- if 'allowed-firmware' in license['license']['status'] -%}
                        &#10004;
                {%- else -%}
                        &nbsp;
                {%- endif -%}
               </td>
               <td>
                {{ license['license']['status']|join(',') }}
               </td>
               <td><a href="{{ spdx_url(license) }}">{{ license['license']['expression'] }}</a></td>
               <td>
                {%- if 'fedora' in license %}
                        {{ license['fedora']['name']|join(',') }}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
               <td>
                {%- if 'fedora' in license -%}
                        {{ license['fedora']['abbreviation']|join(',') }}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
                <td>
                {{ additional_urls(license) }}
                </td>
            </tr>
{%- endfor -%}
 </tbody>
</table>
+++


= Not Allowed


+++                                                                                                                                                                                          
<table id="notallowed" class="display" style="width:100%">
        <thead>
            <tr>
                <th>SPDX Expression</th>
                <th>Name</th>
                <th>URLs</th>
                <th>Notes</th>
            </tr>
        </thead>
        <tbody>
{%- for license in NOTALLOWED_LICENSES -%}
            <tr>
               <td><a href="{{ spdx_url(license) }}">{{ license['license']['expression'] }}</a></td>
               <td>
                {%- if 'fedora' in license -%}
                        {{ license['fedora']['name']|join(',') }}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
                <td>
                {{ additional_urls(license) }}
                </td>
               <td>
                {%- if 'fedora' in license -%}
                        {%- if 'notes' in license['fedora'] -%}
                                {{- license['fedora']['notes']|trim -}}
                        {%- else -%}
                                &nbsp;
                        {%- endif -%}
                {%- else -%}
                        &nbsp;
                {%- endif -%}
                </td>
            </tr>
{%- endfor -%}
</tbody>
        <tfoot>
            <tr>
                <th>SPDX Expression</th>
                <th>Name</th>
                <th>Fedora Abbreviation</th>
                <th>URLs</th>
            </tr>                                                                                                                                                                            
        </tfoot>
    </table>
+++

+++
<script>
$(document).ready(function () {
    $('#allowed').DataTable({
        order: [[1, 'asc']],
    });
});
$(document).ready(function () {
    $('#all').DataTable({
        order: [[1, 'asc']],
    });
});
$(document).ready(function () {
    $('#notallowed').DataTable({
        order: [[1, 'asc']],
    });
});
</script>
+++
