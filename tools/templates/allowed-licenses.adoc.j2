= *Allowed* Licenses

This page lists the licenses that are *allowed* for use in Fedora. If a license is marked as *allowed*, it is allowed for any use. If a license is marked as *allowed-fonts*, *allowed-content*, *allowed-documentation*, or *allowed-firmware*, then it is allowed only for certain categories of material as described in xref:license-approval.adoc[License Approval]. This information is generated from data maintained in the https://gitlab.com/fedora/legal/fedora-license-data[Fedora License Data] repository.


[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
|SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in ALLOWED_LICENSES %}
| {{ spdx_url(license) }}
 | {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 | 
  {%- if 'fedora' in license -%}
          {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
          {nbsp}
  {%- endif -%}
 |  {{ additional_urls(license) }}
{%- endfor %}
|===


= *Allowed* Content Licenses

[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
|SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in CONTENT_LICENSES -%}
| {{ spdx_url(license) }}
 | {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |
  {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |  {{ additional_urls(license) }}
{%- endfor %}
|===

= *Allowed* Documentation Licenses

[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
|SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in DOCUMENTATION_LICENSES -%}
| {{ spdx_url(license) }}
 | {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |
  {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |  {{ additional_urls(license) }}
{%- endfor %}
|===

= *Allowed* Font Licenses

[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
|SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in FONTS_LICENSES %}
| {{ spdx_url(license) }}
 | {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |
  {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |  {{ additional_urls(license) }}
{%- endfor %}
|===


= *Allowed* Firmware Licenses

[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
|SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in FIRMWARE_LICENSES -%}
| {{ spdx_url(license) }}
 | {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        &nbsp;
  {%- endif -%}
 |
  {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
 |  {{ additional_urls(license) }}
{%- endfor %}
|===


= All licenses

[.datatable.dt-search]
[.datatable.dt-search.dt-paging]
|===
| Allowed | Allowed-content | Allowed-documentation | Allowed-fonts | Allowed-firmware | Status | SPDX Expression | Name | Fedora Abbreviation | URLs

{% for license in ALL_ALLOWED_LICENSES -%}
| {%- if 'allowed' in license['license']['status'] -%}
        &#10004;
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {%- if 'allowed-content' in license['license']['status'] -%}
        &#10004;
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {%- if 'allowed-documentation' in license['license']['status'] -%}
        &#10004;
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {%- if 'allowed-fonts' in license['license']['status'] -%}
        &#10004;
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {%- if 'allowed-firmware' in license['license']['status'] -%}
        &#10004;
  {%- else -%}
        {nbsp}
  {%- endif -%}
|  {{ license['license']['status']|join(',') }}
| {{ spdx_url(license) }}
| {%- if 'fedora' in license %}
        {{ license['fedora']['legacy-name']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {%- if 'fedora' in license -%}
        {{ license['fedora']['legacy-abbreviation']|join(',') }}
  {%- else -%}
        {nbsp}
  {%- endif -%}
| {{ additional_urls(license) }}
{%- endfor %}
|===
